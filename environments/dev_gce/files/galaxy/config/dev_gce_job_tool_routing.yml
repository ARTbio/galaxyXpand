# job_tool_routing generated by galaxy_tools_export.py
# Default destination: cluster_1
# Source priority 1: existing group_vars/all/tools (per-tool override)
# Review before commit.
job_tool_routing:

  # --- ARTbio tools ---
  - id: "bowtieForSmallRNA"
    destination: "cluster_1"  # repo: sr_bowtie
  - id: "retrieve_fasta_from_NCBI"
    destination: "cluster_1"  # repo: fetch_fasta_from_ncbi

  # --- SAMTOOLS ---
  - id: "samtools_view"
    destination: "cluster"  # repo: samtools_view  src: existing

  # --- Data Managers ---
  - id: "data_manager_fetch_genome_all_fasta_dbkey"
    destination: "cluster_1"  # repo: data_manager_fetch_genome_dbkeys_all_fasta
  - id: "bowtie_index_builder_data_manager"
    destination: "local_env"  # repo: data_manager_bowtie_index_builder  src: existing
