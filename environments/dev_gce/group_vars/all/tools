# job_tool_routing generated by galaxy_tools_export.py
# Effective default destination (skipped from emission): local_env
# Source priority 1: group_vars/all/job_conf (per-tool override)
# Only tools with explicit overrides are emitted; others inherit job_default_destination.
job_tool_routing:

  # --- ARTbio tools ---
  - id: "retrieve_fasta_from_NCBI"
    destination: "cluster"  # repo: fetch_fasta_from_ncbi  src: existing
  - id: "bowtieForSmallRNA"
    destination: "cluster"  # repo: sr_bowtie  src: existing

  # --- SAMTOOLS ---
  - id: "samtools_view"
    destination: "cluster"  # repo: samtools_view  src: existing
